# 바탕화면 키우기 (Desktop Companion) — 개발 기획서

> 픽셀 그래픽의 귀여운 사람형 캐릭터가 바탕화면 위에서 살아가는 데스크톱 컴패니언 게임.
> 혼자서는 반려 캐릭터로, 친구와 연결하면 최대 4인의 캐릭터가 한 화면에서 함께 지내는 소셜 경험으로 확장.

---

## 1. 제품 개요

### 1.1 컨셉 한 줄 요약
"내 바탕화면에 사는 작은 룸메이트" — 작업 중인 나를 지켜보고, 옆에서 같이 일하고, 친구의 캐릭터와 만나 수다를 떠는 데스크톱 펫.

### 1.2 핵심 가치
| 가치 | 설명 |
|------|------|
| 정서적 동반 | 혼자 작업할 때의 외로움을 덜어주는 살아있는 존재감 (ADL — Activities of Daily Living 기반 자율 행동) |
| 나만의 캐릭터 | 픽셀 파츠 조합 커스터마이징으로 애착 형성 |
| 가벼운 소셜 | 최대 4인 연결, 말풍선 채팅, 화면 엿보기 등 부담 없는 원격 동석(co-presence) 경험 |
| 방해하지 않음 | 클릭 통과(click-through) 오버레이로 실제 작업을 절대 방해하지 않는 것이 대원칙 |

### 1.3 타겟 사용자
- 재택/원격 근무자, 학생 등 장시간 PC 앞에 있는 사람
- 친구·연인·스터디 그룹과 "같이 있는 느낌"을 원하는 사람 (Discord 상시 통화의 라이트 버전)
- 데스크톱 꾸미기/펫 육성(Shimeji, Desktop Goose, Stray 등) 팬층

---

## 2. 기능 명세

### 2.1 캐릭터 자율 행동 (싱글 플레이 코어)

캐릭터는 **행동 상태 머신(FSM) + 유틸리티 기반 행동 선택**으로 스스로 살아간다.

#### 자율 행동 목록 (Idle Behaviors)
| 행동 | 트리거 | 상세 |
|------|--------|------|
| 배회 (Wander) | 기본 상태, 랜덤 | 바탕화면을 천천히 걸어다님. 화면 하단(작업표시줄 위) 및 열린 창들의 상단 모서리를 "바닥"으로 인식해 올라설 수 있음 |
| 창 쳐다보기 (Watch) | 사용자가 활성 창 전환/스크롤 시 | 현재 포커스된 창 방향으로 이동해 창 모서리에 앉아 안을 들여다보는 애니메이션 |
| 같이 일하기 (Co-work) | 사용자의 키보드/마우스 활동이 일정 시간 지속 | 활성 앱 카테고리를 감지해 어울리는 동작 수행: IDE·문서 → 미니 노트북 타이핑, 브라우저 → 책 읽기, 디자인 툴 → 그림 그리기, 음악 앱 → 리듬 타기 |
| 도와주기 (Assist) | Co-work 중 확률적 발동 | 커서 근처로 와서 응원 이모트, 장시간 작업 시 스트레칭 권유 말풍선, 정각 알림 등 가벼운 개입 |
| 일상 행동 (ADL) | 내부 욕구 게이지 | 낮잠, 간식 먹기, 스트레칭, 창밖 보기(화면 가장자리), 하품, 앉아서 쉬기 등 — 배고픔/피로/기분 게이지가 임계치를 넘으면 자율 수행 |

#### 우클릭 명령 (Commands)
캐릭터 우클릭 → 픽셀 스타일 컨텍스트 메뉴:

1. **마우스 따라다니기 (Follow)**
   - 캐릭터 본인의 페이스(걷기 속도)로 커서를 따라옴
   - 커서와의 거리가 임계값(예: 화면 폭의 40%)을 넘으면 **달리기** 전환
   - 달리기는 **스태미나**를 소모 → 소진 시 멈춰서 헥헥거리며 회복 대기 (지침 상태)
   - 다시 우클릭 → "그만 따라와" 로 해제
2. **낮잠자기 (Nap)** — 그 자리에 눕거나 창 모서리에 기대어 잠. Zzz 이펙트. 깨우기 전까지 유지(클릭으로 깨움)
3. **여기서 기다려 (Stay)** — 지정 위치에 대기
4. **간식 주기 (Feed)** — 배고픔 게이지 회복, 호감도 상승
5. **쓰다듬기 (Pet)** — 하트 이모트, 기분 게이지 상승
6. 기타: 옷장 열기(커스터마이징 화면), 설정, 숨기기(트레이로)

#### 내부 스탯 시스템
| 게이지 | 감소 요인 | 회복 요인 | 임계 행동 |
|--------|-----------|-----------|-----------|
| 스태미나 | 달리기, 오래 걷기 | 서서 쉬기, 낮잠 | 소진 시 지침 상태(이동 불가, 헥헥) |
| 배고픔 | 시간 경과 | 간식 주기, 스스로 간식 찾아먹기 | 배고프면 간식 조르기 말풍선 |
| 기분 | 방치, 배고픔 지속 | 쓰다듬기, 친구 캐릭터와 놀기, Co-work | 낮으면 시무룩한 자세/이모트 |
| 졸림 | 시간 경과(특히 사용자 로컬 시간 밤) | 낮잠 | 졸리면 스스로 낮잠 시도 |

> 스탯은 "관리 실패 시 벌칙"이 아니라 **행동 다양성의 연료**로만 사용한다. 죽거나 가출하는 등의 네거티브 디자인은 배제 (방치형 부담 최소화).

### 2.2 캐릭터 커스터마이징

- 최초 실행 시 **캐릭터 메이커** 진입. 이후 언제든 "옷장" 메뉴로 재수정 가능.
- 픽셀 아트, 사람 형태(치비 비율, 예: 32×48px 논리 해상도 → 정수배 스케일 렌더).
- **레이어드 파츠 시스템**:

| 슬롯 | 예시 옵션 수 (MVP) | 비고 |
|------|--------------------|------|
| 피부톤 | 8 | 팔레트 스왑 |
| 눈 | 12 | 모양 + 색상 분리 |
| 코 | 6 | |
| 입 | 10 | 표정 애니메이션과 연동 |
| 머리스타일 | 16 | 앞머리/뒷머리 레이어 분리, 염색(팔레트 스왑) |
| 상의 | 12 | |
| 하의 | 12 | |
| 신발 | 8 | |
| 악세사리 | 10 | 안경, 모자, 헤드폰 등 — 모자는 머리 레이어와 z-order 규칙 필요 |

- 구현 방식: 파츠별 스프라이트 시트를 동일한 뼈대(애니메이션 프레임 좌표)에 맞춰 제작 → 런타임에 레이어 합성. 색상은 그레이스케일 원본 + 팔레트 스왑 셰이더(또는 사전 합성 캐시)로 처리해 아트 비용 절감.
- 커스터마이징 결과는 JSON(슬롯별 파츠 ID + 색상)으로 직렬화 → 로컬 저장 + 멀티플레이 시 상대에게 전송(상대 클라이언트가 동일 규칙으로 합성 렌더).
- **모든 파츠는 잠금 없이 개방** (buy-to-play 원칙, §7). 기본 파츠와 유저 제작 모드 파츠(§8)는 엔진 관점에서 동일한 매니페스트 포맷 — 옷장에 나란히 노출된다.

### 2.3 멀티플레이 (최대 4인)

#### 연결 모델
- **친구 연결은 상호 동의 필수**: A가 친구 코드/링크로 요청 → B가 수락해야 성립.
- **룸(Room)** 단위로 최대 4명. 룸에 참여하면 각자의 바탕화면에 나머지 참여자들의 캐릭터가 나타남.
- 각자는 자기 화면에서 자기 캐릭터를 띄우고 있고, 그 캐릭터의 상태(위치·행동·꾸미기)가 다른 참여자 화면에 미러링됨.

#### 위치 동기화 문제 (핵심 설계 이슈)
서로 해상도·모니터 구성이 다르므로 절대 좌표 동기화는 불가.
- **정규화 좌표**(0.0~1.0, 주 모니터 기준)로 전송하고 각 클라이언트가 자기 해상도로 역변환.
- 원격 캐릭터의 "창 위에 올라서기" 등 로컬 환경 의존 행동은 **수신 측에서 재해석**: 원격 캐릭터는 수신자 화면의 바닥/창 모서리에 스냅. 즉 완전 미러링이 아니라 "행동 의도(intent) 동기화" — `walking_to(x_norm)`, `napping`, `emote(heart)` 수준의 이벤트를 보내고 세부 연출은 로컬에서 수행.
- 이 방식은 네트워크 트래픽도 최소화함 (초당 수십 바이트 수준).

#### 캐릭터 간 상호작용
- 근접 시 자동 상호작용: 인사 이모트, 손 흔들기, 하이파이브, 같이 앉아 수다(말풍선 없는 웅얼거림 이펙트), 술래잡기 미니 인터랙션.
- 상대 캐릭터 클릭 시 인터랙션 메뉴: 인사하기, 하이파이브 요청, 같이 놀기 등.

#### 말풍선 채팅
- **입력**: 전역 단축키(기본 `Ctrl+Shift+Space`, 변경 가능) 또는 내 캐릭터 클릭 → 캐릭터 머리 위 입력창.
- **표시**: 보낸 사람 캐릭터 머리 위 픽셀 말풍선. 글자 수 비례 시간(기본 5~12초) 후 페이드아웃.
- **기록**: 상대(또는 내) 캐릭터 좌클릭 → 대화 기록 패널(작은 픽셀 창)에서 지난 채팅 스크롤 조회. 기록은 로컬 암호화 저장, 보존 기간 설정 가능(기본 7일).
- 이모지/짧은 스티커(이모트) 지원. 도배 방지 rate limit.

### 2.4 화면 엿보기 (Screen Peek) — 시그니처 기능

> "상대 캐릭터가 작은 창을 들고 있고, 마우스를 올리면 상대 화면 일부가 보인다"

#### 플로우
1. 상대 캐릭터 **우클릭 → "화면 보여줘 요청"**.
2. 상대 화면에 요청 알림(상대 캐릭터가 내 캐릭터에게 쪽지를 전달하는 연출) → **명시적 승인 필요**.
3. 승인 시: 이후 내가 상대 캐릭터에 **마우스 hover** → 상대 캐릭터가 작은 액자/창을 번쩍 들어올리는 애니메이션 → 그 창 안에 상대 화면의 일부가 실시간 표시.
4. hover 해제 → 창 내리고 스트림 일시정지.

#### 공유 범위 규칙
- 공유되는 영역은 **송신자 화면에서 송신자 본인의 캐릭터가 들고 있는(위치한) 지점 주변의 고정 크기 영역**(예: 480×300 논리 픽셀)에 매핑. 즉 송신자는 자기 캐릭터를 옮겨서 "보여줄 부분"을 스스로 제어할 수 있다.
- 대안 모드(설정): 특정 창(window) 하나만 공유 — OS 창 캡처 API로 해당 창만 캡처하면 다른 창은 절대 노출되지 않음. **프라이버시 기본값은 이 모드를 권장.**

#### 프라이버시 안전장치 (필수)
- 승인은 **세션 단위**(룸 나가면 소멸) + 언제든 원클릭 철회.
- 송신 중에는 송신자 캐릭터에 명확한 상태 표시(방송 중 아이콘) — 몰래 보기 불가.
- hover 시작마다 송신자 측에 은은한 알림 이펙트(누가 지금 보고 있는지 캐릭터 이름표 표시).
- 알림/비밀번호 필드 노출 위험 고지, OS 수준 화면수집 권한 명시적 요청.

#### 기술 방식
- WebRTC P2P 스트림 (시그널링만 서버 경유). 캡처는 화면 전체 캡처 후 크롭 or 창 단위 캡처, 저해상도(≤480p)·저프레임(5~10fps)으로 충분 — "엿보기" 감성에 맞고 대역폭 최소화.

---

## 3. 기술 아키텍처

### 3.1 스택 선정

| 영역 | 선택 | 이유 |
|------|------|------|
| 데스크톱 셸 | **Electron + TypeScript** | 투명·항상위·클릭통과 창(`setIgnoreMouseEvents(true, {forward:true})`), 전역 단축키, 트레이, `desktopCapturer`(화면공유), 크로스플랫폼(Win/macOS) 지원이 가장 성숙. Tauri는 경량이지만 per-pixel 클릭통과와 화면캡처 생태계가 아직 불리 |
| 렌더링 | **PixiJS** (WebGL, pixel-perfect 설정) | 스프라이트 레이어 합성, 팔레트 스왑 필터, 저부하 렌더 루프 |
| 활성 창 감지 | `active-win` + OS별 네이티브 모듈 | 창 전환/위치(bounds) 감지 → "창 쳐다보기", "창 위에 올라서기" |
| 실시간 서버 | **Node.js + Socket.IO** (경량 relay) | 룸 관리, presence, 채팅, 행동 intent 중계, WebRTC 시그널링 |
| 화면 스트림 | WebRTC (P2P) | 서버 부하 없음, 지연 최소 |
| 로컬 저장 | SQLite (better-sqlite3) | 캐릭터 JSON, 설정, 채팅 기록(암호화) |
| 계정/친구 | **Steam 계정 (steamworks.js)** | Steam ID가 곧 계정 — 자체 계정 시스템 불필요. 친구 초대는 Steam 친구 목록 + 보조 친구코드 |
| 모드/UGC | Steam Workshop (UGC API) | 파츠 팩 업로드/구독/자동 업데이트 (§8) |

### 3.2 클라이언트 구조

```
┌─ Electron Main Process ─────────────────────────┐
│  창 관리(투명 오버레이, 클릭통과 토글)          │
│  전역 단축키 / 트레이 / 자동시작               │
│  active-win 폴링, 커서 좌표 폴링(60→10Hz 적응) │
│  desktopCapturer, 권한 관리                     │
└──────────────┬──────────────────────────────────┘
               │ IPC
┌─ Renderer (오버레이 창, PixiJS) ────────────────┐
│  캐릭터 엔진                                    │
│   ├ 행동 FSM + 유틸리티 선택기                  │
│   ├ 스탯 시스템 (스태미나/배고픔/기분/졸림)     │
│   ├ 이동/경로 (바닥·창 모서리 = 플랫폼)         │
│   └ 스프라이트 합성기 (파츠 레이어 + 팔레트)    │
│  원격 캐릭터 프록시 (intent → 로컬 연출 재해석) │
│  UI: 말풍선, 컨텍스트 메뉴, 채팅기록, 피크 창    │
└──────────────┬──────────────────────────────────┘
               │ WebSocket / WebRTC
        ┌──────┴───────┐
        │ Relay Server │  (룸·presence·채팅·시그널링)
        └──────────────┘
```

#### 클릭 통과 처리 (기술 핵심)
- 오버레이는 화면 전체 크기 투명 창, 기본 `ignoreMouseEvents(true, {forward:true})`.
- forward된 mousemove로 커서가 캐릭터 스프라이트의 불투명 픽셀 위에 있는지 hit-test → 올라간 순간만 `ignoreMouseEvents(false)`로 전환해 클릭/우클릭/hover 수신, 벗어나면 다시 통과 모드.
- 결과: 캐릭터 위에서만 상호작용, 그 외 영역은 바탕화면·다른 앱이 100% 정상 동작.

#### 행동 FSM 상태 정의 (v1)
```
Idle ─▶ Wander ─▶ WatchWindow ─▶ CoWork ─▶ Assist
  │        │
  ▼        ▼
 Nap    FollowMouse(walk) ⇄ FollowMouse(run) ─▶ Exhausted ─▶ Idle
  │
 Stay / Feed / Petted / SocialInteract(원격 캐릭터 근접 시)
```
- 전이는 (a) 사용자 명령 최우선, (b) 스탯 임계값, (c) 유틸리티 점수(가중 랜덤) 순으로 결정.
- 모든 상태는 진입/유지/탈출 애니메이션 키를 가짐 → 아트 스펙과 1:1 매핑.

### 3.3 네트워크 프로토콜 (요약)
| 메시지 | 방향 | 페이로드 | 빈도 |
|--------|------|----------|------|
| `presence.join/leave` | C↔S | roomId, 캐릭터 JSON | 이벤트 |
| `char.intent` | C→S→C | 상태명, 정규화 좌표, 방향 | 상태 변화 시 + 이동 중 2~5Hz |
| `chat.bubble` | C→S→C | text(≤200자), ts | 이벤트 |
| `interact.request/accept` | C→S→C | 하이파이브 등 | 이벤트 |
| `peek.request/grant/revoke` | C→S→C | 모드(영역/창) | 이벤트 |
| `webrtc.signal` | C→S→C | SDP/ICE | 피크 세션 시 |

### 3.4 성능 예산 (상시 실행 앱의 생명선)
- CPU: 유휴 시 < 1~2% (렌더 루프 30fps 상한, 캐릭터 정지 시 프레임 스킵/온디맨드 렌더)
- RAM: < 200MB 목표
- 배터리 배려: 노트북 배터리 모드 감지 시 애니메이션 프레임레이트 자동 하향
- GPU 미지원 환경 canvas 폴백

---

## 4. 아트 & 사운드 스펙

- 캐릭터 논리 해상도 32×48px, 4방향 불필요(좌/우 + 정면이면 충분, 픽셀 감성 유지), 정수배 스케일(2x~4x 사용자 설정).
- 필수 애니메이션 (MVP): idle(2종), walk, run, exhausted, sit, nap, wake, eat, pet-reaction, typing(co-work), read, wave, highfive, carry-frame(피크 창 들기), talk(말풍선용 입모양).
- 파츠 아트는 "뼈대 프레임 규격서"에 맞춰 제작 → 신규 파츠 추가가 코드 수정 없이 가능하도록 매니페스트(JSON) 기반 로딩.
- 사운드: 발소리·이모트 효과음 등 미니멀, 기본 음소거 옵션 제공 (업무 방해 금지 원칙).

---

## 5. 개발 로드맵

### Phase 0 — 기술 검증 프로토타입 (2주)
- [ ] 투명·항상위·클릭통과 오버레이 + per-pixel hit-test (Win/macOS 각각 검증) ← **최대 리스크, 최우선**
- [ ] 더미 캐릭터 걷기 + 우클릭 메뉴 + 마우스 따라오기
- [ ] active-win으로 창 bounds 얻어 "창 위에 서기" PoC
- 판정: 위 3개가 두 OS에서 매끄럽지 않으면 스택 재검토

### Phase 1 — 싱글플레이 코어 (4주)
- [ ] 행동 FSM 전체(배회/쳐다보기/Co-work/낮잠/Follow+스태미나/지침)
- [ ] 스탯 시스템 + 시간대 반응
- [ ] 트레이, 자동 시작, 설정 창
- 마일스톤: **"혼자서도 하루 종일 켜두고 싶은가?"** 내부 테스트

### Phase 2 — 커스터마이징 (3주)
- [ ] 캐릭터 메이커 UI + 파츠 합성 파이프라인 + 팔레트 스왑
- [ ] 파츠 매니페스트 시스템 (추후 파츠 추가 = 데이터만 추가)
- [ ] MVP 파츠 세트 아트 제작 (외주/자체)

### Phase 3 — 멀티플레이 (4주)
- [ ] Relay 서버(룸/친구코드/상호 동의), presence, intent 동기화
- [ ] 원격 캐릭터 로컬 재해석 렌더
- [ ] 말풍선 채팅 + 기록 패널 + 근접 상호작용
- 마일스톤: 4인 룸 8시간 연속 안정성 테스트

### Phase 4 — 화면 엿보기 (3주)
- [ ] WebRTC 파이프라인 + 요청/승인/철회 UX
- [ ] 창 단위 공유 모드(프라이버시 기본), 영역 모드
- [ ] 감시 방지 장치(상태 표시, hover 알림)

### Phase 5 — Steam 통합 & 모드 시스템 (3주)
- [ ] steamworks.js 연동: 인증, 친구 목록 기반 룸 초대, 도전과제, Rich Presence
- [ ] 모드 로더: 파츠 팩 폴더 스캔 + 핫리로드, 템플릿 내보내기
- [ ] Workshop 업로드/구독 파이프라인 + 업로드 전 규격 린트
- [ ] 멀티플레이 커스텀 파츠 P2P 전송(해시 캐시)

### Phase 6 — 폴리시 & Steam 출시 준비 (3주)
- [ ] 성능 예산 달성, 크래시 리포팅, 업데이트 배포(Steam 빌드 파이프라인)
- [ ] 온보딩(첫 실행 캐릭터 메이커 → 튜토리얼 대사)
- [ ] Steam 페이지 제작(트레일러·스크린샷), 플레이테스트(Steam Playtest 기능) 50~100명 → 리텐션(D7 앱 재실행률) 측정
- [ ] 시드 모드 파츠 팩 5~10개 자체 제작(Workshop 초기 진열대 채우기)

총 예상: **약 22주 (~5개월)**, 1~2인 개발 + 픽셀 아티스트 1인(파트타임) 기준.

---

## 6. 리스크 & 대응

| 리스크 | 심각도 | 대응 |
|--------|--------|------|
| OS별 오버레이/클릭통과 동작 차이 (macOS 스페이스 전환, Windows 전체화면 게임) | 높음 | Phase 0에서 집중 검증. 전체화면 앱 감지 시 캐릭터 자동 숨김 |
| 화면공유 프라이버시 사고 | 높음 | 창 단위 공유 기본값, 송신 상태 상시 표시, 세션 단위 권한, 민감정보 고지 |
| 상시 실행 앱의 리소스 불만 | 중간 | §3.4 성능 예산을 출시 기준(gate)으로 삼음 |
| active-win 등 네이티브 의존성의 OS 업데이트 취약성 | 중간 | 기능별 graceful degradation (감지 실패 시 배회만 해도 성립하는 설계) |
| 아트 물량 (파츠 × 애니메이션 조합 폭발) | 중간 | 뼈대 규격 통일 + 팔레트 스왑으로 변형 비용 절감, MVP 파츠 수 제한 |
| 멀티플레이 동시성 버그 | 낮음 | intent 동기화(서버는 relay만) 구조라 상태 충돌 표면적이 작음 |

---

## 7. 수익 모델 — Steam 유료 출시 (Buy-to-Play)

- **Steam 단일 구매로 수익화 종료.** 인앱 결제·파츠 판매·구독 없음. 구매 후 모든 기능(커스터마이징 전체, 4인 멀티, 화면 엿보기, 모드) 개방.
- 가격 포지셔닝: $9.99~14.99 구간 (Wallpaper Engine·Desktop Mate 등 데스크톱 상주형 앱의 검증된 가격대). 출시 할인 + 친구와 함께 사는 4-pack 번들(멀티가 핵심 재미이므로 번들이 자연스러운 판매 촉진 장치).
- 콘텐츠 수명은 판매가 아니라 **모드 생태계(§8)**가 담당: 파츠를 직접 그리고 Steam Workshop으로 공유. Wallpaper Engine이 증명한 "저가 buy-to-play + UGC 워크샵 = 롱테일 판매" 모델을 따른다.
- 추후 확장 시에도 과금이 아닌 **무료 업데이트**(시즌 파츠 드롭, 신규 상호작용)로 리뷰 점수와 워크샵 활성도를 관리 — 데스크톱 상주 앱은 Steam 리뷰가 곧 마케팅이다.
- 절대 하지 않을 것: 인앱 결제 일체, 스탯 압박형 디자인, 광고 오버레이 — "방해하지 않음" 원칙과 충돌.

### Steam 플랫폼 통합 (Steamworks)
| 항목 | 내용 |
|------|------|
| SDK 연동 | Electron ↔ Steamworks는 `steamworks.js` (Node 네이티브 바인딩) 사용 |
| Workshop | 파츠 팩 모드의 업로드/구독/자동 업데이트 채널 (§8) |
| 친구 연결 | 룸 초대를 Steam 친구 목록 기반으로 제공 (별도 친구코드도 병행 — 크로스 초대 대비) |
| 도전과제 | "첫 낮잠", "4인 룸 개설", "첫 모드 제작" 등 라이트한 세트 |
| Rich Presence | "캐릭터와 작업 중 (3인 룸)" 표시 |
| 주의점 | Steam 오버레이는 투명 클릭통과 창과 충돌 가능 → 오버레이 비활성 전제로 설계하고 UI는 전부 자체 구현. 상주 앱 특성상 "게임 실행 시간" 통계가 매우 길게 잡히는 점은 오히려 노출에 유리 |

---

## 8. 모드 시스템 & Steam Workshop (콘텐츠 생태계의 핵심)

> 판매는 한 번, 콘텐츠는 커뮤니티가 무한히. §2.2의 파츠 매니페스트 구조가 그대로 모드 포맷이 된다.

### 9.1 모드 포맷 (파츠 팩)
- 모드 = **파츠 팩 폴더**: `manifest.json` (팩 이름, 제작자, 버전, 포함 파츠 목록·슬롯·태그) + 파츠별 스프라이트 시트 PNG.
- 스프라이트는 §4의 **뼈대 프레임 규격서**(프레임 좌표·앵커 규격)만 지키면 됨 — 기본 파츠와 모드 파츠를 엔진이 구분하지 않는다(1급 시민). 팔레트 스왑 가능 여부도 매니페스트 플래그로 선언.
- 파츠 외 확장 여지(v2): 이모트 팩, 말풍선 스킨, 사운드 팩 — 포맷은 v1부터 `type` 필드로 예약.

### 9.2 파츠 제작 지원
- **템플릿 내보내기**: 옷장에서 "파츠 템플릿 내보내기" → 뼈대 가이드가 그려진 PNG 템플릿 + 매니페스트 스켈레톤 생성. Aseprite/Photoshop 등 외부 픽셀 툴로 그린 뒤 폴더에 넣으면 즉시 로드.
- **인게임 미리보기**: 모드 폴더 감시(hot-reload) → 그리는 중에도 캐릭터에 바로 입혀보며 애니메이션 확인 가능. 제작 경험이 곧 리텐션이므로 이 루프를 매끄럽게 만드는 데 투자.
- 인게임 픽셀 에디터는 v2 후보(범위 통제) — v1은 외부 툴 + 템플릿 + 핫리로드로 충분.

### 9.3 Workshop 업로드/구독
- 인게임 "모드 관리" 화면에서 원클릭 Workshop 업로드(steamworks.js UGC API), 구독한 팩은 자동 다운로드·업데이트 후 옷장에 자동 노출.
- 검증 파이프라인: 업로드 전 로컬 린트(프레임 규격, 크기 상한, 투명 영역 검사) → 규격 미달은 업로드 차단으로 저품질/깨진 모드 유통 방지.
- 안전성: 모드는 **데이터(PNG+JSON)만 허용, 코드 실행 없음** — 보안 표면을 원천 차단. 부적절 이미지 신고는 Workshop 기본 신고 체계에 위임.

### 9.4 멀티플레이에서의 모드 파츠 동기화 (설계 포인트)
- 상대가 내 커스텀 파츠를 갖고 있지 않아도 보여야 한다. 파츠는 수 KB 픽셀 아트이므로:
  1. 캐릭터 JSON에 파츠별 **콘텐츠 해시** 포함
  2. 수신 측에 해당 해시 캐시가 없으면 P2P(데이터 채널)로 스프라이트 시트 전송(팩당 상한 예: 256KB) 후 해시 키로 캐시
  3. Workshop 팩이면 workshopId를 함께 보내 "이 팩 구독하기" 바로가기 제공
- 결과: 친구 화면에서 내 자작 옷이 항상 그대로 보이고, 마음에 들면 그 자리에서 구독 — 모드 전파가 소셜 루프에 내장된다.

## 9. 성공 지표
- D7 재실행률 ≥ 40% (상시 실행 앱 특성상 핵심 지표)
- 일 평균 실행 시간 ≥ 3시간
- 멀티 룸 생성 사용자 비율 ≥ 25%, 룸 재접속률 ≥ 50%
- 화면 엿보기 승인율 (기능 신뢰도 프록시) 및 철회율 모니터링
- Steam: 리뷰 긍정률 ≥ 90% (Very Positive 유지 — 상주 앱은 리뷰가 곧 마케팅), 환불률 < 8%
- Workshop: 출시 3개월 내 파츠 팩 100개, 구독 경험 사용자 비율 ≥ 30% (생태계 발화점 프록시)
- 4-pack 번들 판매 비중 (소셜 루프를 통한 자연 확산 지표)

---

## 부록 A. MVP 범위 요약 (한 페이지)
**IN**: 자율 행동 5종, 우클릭 명령(따라오기·낮잠·기다려·간식·쓰다듬기), 스탯 4종, 캐릭터 메이커(9슬롯, 전 파츠 개방), 4인 룸 + 말풍선 채팅 + 기록, 화면 엿보기(창 모드), Steam 출시(buy-to-play) + Workshop 파츠 모드(템플릿 내보내기·핫리로드·업로드/구독), Win/macOS.
**OUT (v2 이후)**: 모바일 뷰어, 캐릭터 간 선물/미니게임, 인게임 픽셀 에디터, 파츠 외 모드 타입(이모트·사운드 팩), Linux, 다중 모니터 간 이동, 음성.
